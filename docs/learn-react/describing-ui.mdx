---
title: "Describing the UI: Props and Rendering"
description: "Master React props, conditional rendering, and lists. The foundations of build dynamic user interfaces."
keywords: [react props, react conditional rendering, react lists, scrimba react]
---

import AffiliateLink from '@site/src/components/AffiliateLink';

# Describing the UI

In this section, you'll learn how to make your components dynamic and reusable using **Props** and **Logic**.

## 1. Passing Props

Props (short for "properties") are how you pass data *down* the tree from a parent component to a child. They are read-only.

**The Concept:**
Imagine a component is a function. Props are the arguments.

```jsx
// Parent
<Profile name="Bob" role="Teacher" />

// Child
function Profile(props) {
  return <h2>{props.name} is a {props.role}</h2>;
}
```

**Learn it on Scrimba:**
-> **[Interactive Lesson: Passing Data to Components](https://scrimba.com/learn/learnreact/props-part-1-c0f?via=u42d4986)**

## 2. Conditional Rendering

Sometimes you want to show different things depending on the state of your app (e.g., a "Login" button vs a "Logout" button).

You can use standard JavaScript logic:
- `if` statements
- Ternary operators (`condition ? true : false`)
- Logical AND (`condition && true`)

**Learn it on Scrimba:**
-> **[Interactive Lesson: Conditional Rendering](https://scrimba.com/learn/learnreact/conditional-rendering-&&-c010?via=u42d4986)**

## 3. Rendering Lists

You will often want to display multiple similar components from a collection of data. In React, you use the JavaScript `.map()` array method for this.

**Key Rule:** Every list item needs a unique `key` prop so React can track it.

```jsx
const items = ["Apple", "Banana", "Cherry"];
const listItems = items.map(item => <li key={item}>{item}</li>);
```

**Learn it on Scrimba:**
-> **[Interactive Lesson: Mapping Components](https://scrimba.com/learn/learnreact/mapping-components-c012?via=u42d4986)**

---

<div style={{ padding: '1rem', border: '1px solid var(--ifm-color-emphasis-200)', borderRadius: '8px', backgroundColor: 'var(--ifm-background-surface-color)' }}>
  <h3>Ready to build something?</h3>
  <p>Practice these concepts by building an Airbnb Experiences clone in the free React course.</p>
  <AffiliateLink href="https://scrimba.com/learn-react-c0e?via=u42d4986" variant="button">
    Start the Project
  </AffiliateLink>
</div>
